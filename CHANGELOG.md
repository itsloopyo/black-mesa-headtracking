# Changelog

Entries are written newest first, one section per released version, headed
`## [X.Y.Z] - YYYY-MM-DD`. `release.ps1` generates them from the commits in
each release. Nothing has been released yet, so there are no entries.
